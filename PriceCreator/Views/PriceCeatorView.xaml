<Window x:Class="PriceCreator.Views.PriceCeatorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PriceCreator.Views"
        mc:Ignorable="d"
        xmlns:vm="clr-namespace:PriceCreator.ViewsModels"
        Title="Price Creator for => rozetka.com.ua" 
        Height="1000" Width="1000"  WindowStartupLocation="CenterScreen"
        >
    <Window.DataContext>
        <vm:PriceCeatorViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="52"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="32"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Grid.Column="0" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Label  Grid.Column="1" FontSize="16" Content="{Binding PathFile}" />
            <Button Command="{Binding OvpenFile }"  Grid.Column="0" FontSize="15" Margin="2" Content="Load/Save" />
        </Grid>
        <GroupBox Grid.Row="1"  Header="Mandatory information" FontSize="16" Margin="10 0 10 10" Background="#DFE5EB" Foreground="#005565" BorderBrush="#005565">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="105"/>
                    <ColumnDefinition Width="14*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="31"/>
                    <RowDefinition Height="31"/>
                    <RowDefinition Height="31"/>
                    <RowDefinition Height="34"/>
                    <RowDefinition Height="31"/>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" Grid.Column="0" Content="Имя"/>
                <TextBox x:Name="tbName" Grid.Row="0" Grid.Column="1" Margin="2" Grid.ColumnSpan="2"/>

                <Label  Grid.Row="1" Grid.Column="0" Content="Компания"/>
                <TextBox  Name="tbCompany" Grid.Row="1" Grid.Column="1" Margin="2" Grid.ColumnSpan="2"/>

                <Label Grid.Row="2" Grid.Column="0" Content="Категории"/>
                <ComboBox   Name="CbCategory" Grid.Row="2" Grid.Column="1" Margin="2" Grid.ColumnSpan="2" />

                <Label Grid.Row="3" Grid.Column="0" Content="Адрес сайта" Margin="2"/>
                <TextBox Name="tbUrl" Grid.Row="3" Grid.ColumnSpan="2" Margin="2"  Grid.Column="1" />


                <Button Grid.Row="4" Grid.Column="0"  FontSize="15" Margin="2" Content="+" />
                <TextBox Name="tbcotegory"  Grid.Row="4" Grid.Column="1" Margin="2" Grid.ColumnSpan="2" />

            </Grid>

        </GroupBox>

        <GroupBox Grid.Row="2"  Header="Products" FontSize="16" Margin="10 0 10 10" Background="#DFE5EB" Foreground="#005565" BorderBrush="#005565">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="350*"/>
                    <RowDefinition Height="38*"/>
                </Grid.RowDefinitions>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Path=Offers}" Name="ICProduct">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="30" />
                                        <ColumnDefinition Width="100*" />
                                    </Grid.ColumnDefinitions>
                                    <CheckBox Grid.Column="0" IsChecked="{Binding Active}" Margin="5"/>
                                    <TextBox  Grid.Column="1" Text="{Binding Text,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Margin="3"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" >
                    <Button Command="{Binding Path=AddCommand }" Background="Green" Margin="0,10,0,0">Добавить</Button>
                    <Button Command="{Binding Path=DeleteCommand }"  Background="Red" Margin="5,10,0,0" Content="Удалить" Width="85" />
                </StackPanel>

            </Grid>
        </GroupBox>

        <StatusBar Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="6" DockPanel.Dock="Bottom" Margin="0" Height="32" Background="#DFE5EB">
            <TextBlock x:Name="Footer" Text="Footer" Margin="3 0 3 0" Padding="0" Background="#DFE5EB"/>
            <StatusBarItem HorizontalAlignment="Right" Background="#E4E8ED">
                <ResizeGrip Opacity="0.75" Background="#E4E8ED"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
